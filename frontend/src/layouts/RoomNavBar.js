import { NavLink } from "react-router-dom";

const RoomNavBar = () => {
  return (
    <nav className="project-nav-bar">
      <NavLink to="">Room boundaries</NavLink>
    </nav>
  );
};

export default RoomNavBar;
